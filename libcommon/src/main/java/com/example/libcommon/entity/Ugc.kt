package com.example.libcommon.entity

/*
* 
* */
data class Ugc(
        var likeCount: Int,
        var shareCount: Int,
        var commentCount: Int,
        var hasFavorite: Boolean,
        var hasdiss: Boolean,
        var hasLiked: Boolean
)